import React from 'react';
declare type titleProps = {
    text: string;
    tooltip?: React.ReactNode;
};
export default function Title({ text, tooltip, }: titleProps): React.ReactElement;
export {};
